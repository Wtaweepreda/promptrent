[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run db:generate && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
